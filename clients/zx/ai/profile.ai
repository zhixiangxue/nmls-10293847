# zx — clerk

> Maintained by clerk · as of 2306d09 · 2026-08-01
> Verifiable facts with sources. Judgement and strategy live in PROFILE.md.

## Loan
purpose: unknown — no document specifies the loan purpose
loan amount: unknown — no document specifies the loan amount
stage: unknown — no document specifies the loan stage
location: unknown — no document specifies the property location
occupancy: unknown — no document specifies the occupancy
target program: unknown — no document specifies the target program

## Borrowers
unknown — no document specifies borrower details

## Income
qualifying income: unknown — no document specifies qualifying income
front-end DTI: unknown — no document specifies front-end DTI
back-end DTI: unknown — no document specifies back-end DTI
income evidence: unknown — no document specifies what income evidence consists of

## Credit
FICO: unknown — no document specifies FICO
credit pull date: unknown — no document specifies when the credit was pulled

## Property
type: unknown — no document specifies the property type
purchase price: unknown — no document specifies the purchase price
appraisal status: unknown — no document specifies the appraisal status

## Documents on file
za: Amazon EC2 Instance Comparison.csv

## Open items
unknown — no document specifies what is missing or owed

## Context
unknown — no document provides context outside structured fields
