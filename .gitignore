# OS junk — Finder drops these everywhere, never business content
.DS_Store
tmp/
.venv/
/session.json

# seeka memory — local state, ignored for now
.seeka/

# local index database
.index.db

# chak runtime state
.chak/
